server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # Enrutamiento del lado del cliente (SPA): cualquier ruta cae en index.html.
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Cacheo prolongado para los assets con hash de Vite.
    location /assets/ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
}
